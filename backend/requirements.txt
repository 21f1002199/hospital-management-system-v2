# Core framework & DB
Flask==2.2.5
Flask-SQLAlchemy==3.0.3
Flask-Migrate==4.0.4

# Auth & security
Flask-JWT-Extended==4.4.4
passlib==1.7.4
email-validator==1.3.1

# API & CORS
Flask-Cors==4.0.0
flask-restx==1.1.0

# Background jobs & cache
redis>=4.5.2,<4.5.5
celery[redis]==5.3.1
Flask-Caching==2.0.1

# Email, exports, utilities
Flask-Mail==0.9.1
python-dotenv==1.0.0
requests==2.31.0

# Optional helpers (uncomment to install)
# Flask-Excel==0.0.7        # convenience CSV helpers (optional)
# WeasyPrint==57.1          # HTML->PDF (requires system libs: Cairo, Pango, GDK-PixBuf)
# flask-security-too==5.0.1 # full-featured auth (opinionated)
# pandas==2.2.3             # optional for heavy CSV/data handling
